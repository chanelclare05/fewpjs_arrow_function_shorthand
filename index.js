

let divide = (num1, num2) => {
    if (num2 > 0) {
    return num1/num2;
    } else {return 20}
}
const num1 = 2000
const num2 = 100

let square = (num1) => num1 * num1;

let add = (num1, num2) => num1 + num2;